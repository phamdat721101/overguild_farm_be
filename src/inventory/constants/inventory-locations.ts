export enum InventoryLocation {
  STORAGE = "STORAGE", // Main storage (unlimited)
  BACKPACK = "BACKPACK", // Backpack (limited capacity)
}

export const BACKPACK_CAPACITY = 20; // Maximum slots in backpack
